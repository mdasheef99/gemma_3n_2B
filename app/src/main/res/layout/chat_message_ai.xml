<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="8dp"
    android:gravity="start">

    <!-- AI avatar -->
    <ImageView
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginEnd="8dp"
        android:background="@drawable/ai_avatar_background"
        android:src="@drawable/ic_robot"
        android:padding="8dp"
        android:layout_gravity="bottom" />

    <!-- AI message bubble aligned to left -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginEnd="64dp"
        android:orientation="vertical">

        <!-- Message bubble container -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="start"
            android:background="@drawable/ai_message_bubble"
            android:orientation="vertical"
            android:padding="12dp"
            android:layout_marginBottom="4dp">

            <!-- Image indicator (shown when message was response to image) -->
            <TextView
                android:id="@+id/imageIndicator"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="🖼️ "
                android:textColor="@color/teal_700"
                android:textSize="14sp"
                android:layout_marginBottom="4dp"
                android:visibility="gone" />

            <!-- Message text with enhanced multilingual support -->
            <TextView
                android:id="@+id/messageText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="AI response text"
                android:textColor="@color/md_on_secondary_container"
                android:textSize="16sp"
                android:lineSpacingExtra="4dp"
                android:maxWidth="320dp"
                android:fontFamily="sans-serif"
                android:textIsSelectable="true"
                android:longClickable="true" />

        </LinearLayout>

        <!-- Timestamp -->
        <TextView
            android:id="@+id/messageTime"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="start"
            android:text="12:34"
            android:textColor="@android:color/darker_gray"
            android:textSize="12sp"
            android:layout_marginStart="8dp" />

    </LinearLayout>

</LinearLayout>
